---
# ------------------------------
title: "MARK 1 (beta)"               # Beta
# ------------------------------
output: 
  flexdashboard::flex_dashboard:
    orientation: rows
    vertical_layout: scroll
    logo: GDOTLogo.svg
    favicon: GDOTLogo.svg 
    css: style.css
    includes:
        in_header: google-analytics.html
runtime: shiny
---


```{r global, cache = FALSE, include = FALSE, warning = FALSE}
# FLEXDASHBOARD - RTOP_MONTHLY_REPORT

options(warn = -1)

source("Monthly_Report_UI_Functions.R")


FIG_WIDTH = 14
FIG_HEIGHT = 7

AWS_BUCKET = "https://s3.amazonaws.com/gdot-spm"

```









Inputs {.sidebar}
=====================================

Measurement, Accuracy, and Reliability Kit (MARK 1) 

Past month reports can be viewed back to `r format(first_month, "%B %Y")`.


RTOP Concept of Operations can be found 
`r tags$a(href = "https://s3.amazonaws.com/gdot-spm/GDOT_STORM-COO.pdf", 
           target = "_blank",
           "here")`


```{r sidebar, warning = FALSE, cache = FALSE}
# shiny inputs defined here



selectInput("month", "Month:",
                       choices = month_options,
                       selected = month_options[1]) # Default to current month

selectInput("zone_group", "Signal Group:",
                       choices = zone_group_options,
                       selected = "All RTOP")



# DRILL DOWN WITHIN CORRIDORS TO SEE ALL SIGNALS
conditionalPanel("input.zone_group == 'RTOP1'",
                 selectInput("corridor_rtop1", "Corridor",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone_Group == "RTOP1")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'RTOP2'",
                 selectInput("corridor_rtop2", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone_Group == "RTOP2")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Zone 1'",
                 selectInput("corridor_z1", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "Zone 1")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Zone 2'",
                 selectInput("corridor_z2", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "Zone 2")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Zone 3'",
                 selectInput("corridor_z3", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "Zone 3")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Zone 4'",
                 selectInput("corridor_z4", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "Zone 4")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Zone 5'",
                 selectInput("corridor_z5", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "Zone 5")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Zone 6'",
                 selectInput("corridor_z6", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "Zone 6")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Zone 7'",
                 selectInput("corridor_z7", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone %in% c("Zone 7m", "Zone 7d"))$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Zone 7m'",
                 selectInput("corridor_z7m", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "Zone 7m")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Zone 7d'",
                 selectInput("corridor_z7d", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "Zone 7d")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Zone 8'",
                 selectInput("corridor_z8", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "Zone 8")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'District 1'",
                 selectInput("corridor_d1", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "District 1")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'District 2'",
                 selectInput("corridor_d2", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "District 2")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'District 3'",
                 selectInput("corridor_d3", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "District 3")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'District 4'",
                 selectInput("corridor_d4", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "District 4")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'District 5'",
                 selectInput("corridor_d5", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "District 5")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'District 6'",
                 selectInput("corridor_d6", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "District 6")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'District 7'",
                 selectInput("corridor_d7", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "District 7")$Corridor
                                         ))),
                             selected = "All Corridors"))

conditionalPanel("input.zone_group == 'Cobb County'",
                 selectInput("corridor_cb", "Corridor:",
                             choices = c("All Corridors", 
                                         as.character(unique(filter(
                                             corridors, Zone == "Cobb County")$Corridor
                                         ))),
                             selected = "All Corridors"))

current_month <- reactive(lubridate::dmy(paste(1, input$month)))
endof_current_month <- reactive(lubridate::dmy(paste(1, input$month)) + months(1) - days(1))
current_quarter <- reactive(as.character(lubridate::quarter(current_month(), with_year = TRUE)))


corridor <- reactive(
    if (input$zone_group == "RTOP1") {
        input$corridor_rtop1
    } else if (input$zone_group == "RTOP2") {
        input$corridor_rtop2
    } else if (input$zone_group == "Zone 1") {
        input$corridor_z1
    } else if (input$zone_group == "Zone 2") {
        input$corridor_z2
    } else if (input$zone_group == "Zone 3") {
        input$corridor_z3
    } else if (input$zone_group == "Zone 4") {
        input$corridor_z4
    } else if (input$zone_group == "Zone 5") {
        input$corridor_z5
    } else if (input$zone_group == "Zone 6") {
        input$corridor_z6
    } else if (input$zone_group == "Zone 7") {
        input$corridor_z7
    } else if (input$zone_group == "Zone 7m") {
        input$corridor_z7m
    } else if (input$zone_group == "Zone 7d") {
        input$corridor_z7d
    } else if (input$zone_group == "Zone 8") {
        input$corridor_z8
    } else if (input$zone_group == "District 1") {
        input$corridor_d1
    } else if (input$zone_group == "District 2") {
        input$corridor_d2
    } else if (input$zone_group == "District 3") {
        input$corridor_d3
    } else if (input$zone_group == "District 4") {
        input$corridor_d4
    } else if (input$zone_group == "District 5") {
        input$corridor_d5
    } else if (input$zone_group == "District 6") {
        input$corridor_d6
    } else if (input$zone_group == "District 7") {
        input$corridor_d7
    } else if (input$zone_group == "Cobb County") {
        input$corridor_cb
    } else {
        "All Corridors"
    })

    
zone_group <- reactive(
    if (corridor() == "All Corridors") {
        input$zone_group
    } else {
        corridor()
    }
)


# mr <- reactive(
#     if (corridor() == "All Corridors") {
#         cor
#     } else {
#         sig
#     }
# )


filtered_corridors <- reactive({

    # All RTOP is the union of RTOP1, RTOP2
    if (input$zone_group == "All RTOP") {
        corr <- corridors %>% 
            filter(Zone_Group %in% c("RTOP1", "RTOP2"))

    # Zone 7 is the union of Zone 7m, 7d
    } else if (input$zone_group == "Zone 7") {
        corr <- corridors %>% 
            filter(grepl("^Zone 7", Zone))

    # Zones filter by Zone rather than Zone_Group
    } else if (startsWith(input$zone_group, "Zone")) {
        corr <- corridors %>% 
            filter(Zone %in% input$zone_group)

    } else {
        corr <- corridors %>% 
            filter(Zone_Group %in% input$zone_group)
    }

    if (corridor() != "All Corridors") { # if a specific corridor is selected, filter on that
        corr %>% filter(Corridor == corridor())
    } else {
        corr
    } %>% select(-Description, -Asof)
})


filtered_signalids <- reactive(paste0(filtered_corridors()$SignalID, ": ",
                                      filtered_corridors()$Name))

# renderText({"\nSelected Month"})
# renderPrint({current_month()})
# 
# renderText({"\nEnd of Selected Month"})
# renderPrint({endof_current_month()})
# 
# renderText({"Selected Zone Group"})
# renderPrint({zone_group()})
# 
# renderText({"Selected Corridor"})
# renderPrint({corridor()})
# 
# renderText({"Selected Querter"})
# renderPrint({current_quarter()})


```


```{r}

mr_str <- reactive(
    if (corridor() == "All Corridors") {
        "cor"
    } else {
        "sig"
    }
)

#m1dynq_ <- memoize(m1dynq)

# dy_du <- m1dynq_(mr_str(), "dy", "du", first_month, endof_current_month())
# dy_cu <- m1dynq_(mr_str(), "dy", "cu", first_month, endof_current_month())
# dy_pau <- m1dynq_(mr_str(), "dy", "pau", first_month, endof_current_month())
# dy_cctv <- m1dynq_(mr_str(), "dy", "cctv", first_month, endof_current_month())
# 
# wk_vpd <- m1dynq_(mr_str(), "wk", "vpd", first_month, endof_current_month())
# wk_vph <- m1dynq_(mr_str(), "wk", "vph", first_month, endof_current_month())
# wk_vphpa <- m1dynq_(mr_str(), "wk", "vphpa", first_month, endof_current_month())
# wk_vphpp <- m1dynq_(mr_str(), "wk", "vphpp", first_month, endof_current_month())
# wk_papd <- m1dynq_(mr_str(), "wk", "papd", first_month, endof_current_month())
# wk_paph <- m1dynq_(mr_str(), "wk", "paph", first_month, endof_current_month())
# wk_tp <- m1dynq_(mr_str(), "wk", "tp", first_month, endof_current_month())
# wk_aog <- m1dynq_(mr_str(), "wk", "aog", first_month, endof_current_month())
# wk_qs <- m1dynq_(mr_str(), "wk", "qs", first_month, endof_current_month())
# wk_sf <- m1dynq_(mr_str(), "wk", "sf", first_month, endof_current_month())
# wk_cu <- m1dynq_(mr_str(), "wk", "cu", first_month, endof_current_month())
# wk_pau <- m1dynq_(mr_str(), "wk", "pau", first_month, endof_current_month())
# wk_cctv <- m1dynq_(mr_str(), "wk", "cctv", first_month, endof_current_month())
# 
# mo_vpd <- m1dynq_(mr_str(), "mo", "vpd", first_month, current_month())
# mo_vph <- m1dynq_(mr_str(), "mo", "vph", first_month, current_month())
# mo_vphpa <- m1dynq_(mr_str(), "mo", "vphpa", first_month, current_month())
# mo_vphpp <- m1dynq_(mr_str(), "mo", "vphpp", first_month, current_month())
# mo_papd <- m1dynq_(mr_str(), "mo", "papd", first_month, current_month())
# mo_paph <- m1dynq_(mr_str(), "mo", "paph", first_month, current_month())
# mo_tp <- m1dynq_(mr_str(), "mo", "tp", first_month, current_month())
# mo_aogd <- m1dynq_(mr_str(), "mo", "aogd", first_month, current_month())
# mo_aogh <- m1dynq_(mr_str(), "mo", "aogh", first_month, current_month())
# mo_qsd <- m1dynq_(mr_str(), "mo", "qsd", first_month, current_month())
# mo_qsh <- m1dynq_(mr_str(), "mo", "qsh", first_month, current_month())
# mo_sfd <- m1dynq_(mr_str(), "mo", "sfd", first_month, current_month())
# mo_sfh <- m1dynq_(mr_str(), "mo", "sfh", first_month, current_month())
# mo_tti <- m1dynq_(mr_str(), "mo", "tti", first_month, current_month())
# mo_ttih <- m1dynq_(mr_str(), "mo", "ttih", first_month, current_month())
# mo_pti <- m1dynq_(mr_str(), "mo", "pti", first_month, current_month())
# mo_ptih <- m1dynq_(mr_str(), "mo", "ptih", first_month, current_month())
# mo_du <- m1dynq_(mr_str(), "mo", "du", first_month, current_month())
# mo_cu <- m1dynq_(mr_str(), "mo", "cu", first_month, current_month())
# mo_pau <- m1dynq_(mr_str(), "mo", "pau", first_month, current_month())
# mo_veh <- m1dynq_(mr_str(), "mo", "veh", first_month, current_month())
# mo_ped <- m1dynq_(mr_str(), "mo", "ped", first_month, current_month())
# mo_cctv <- m1dynq_(mr_str(), "mo", "cctv", first_month, current_month())
# mo_events <- m1dynq_(mr_str(), "mo", "events", first_month, current_month())
# 
# qu_vpd <- m1dynq_(mr_str(), "qu", "vpd", current_quarter())
# qu_vph <- m1dynq_(mr_str(), "qu", "vph", current_quarter())
# qu_vphpa <- m1dynq_(mr_str(), "qu", "vphpa", current_quarter())
# qu_vphpp <- m1dynq_(mr_str(), "qu", "vphpp", current_quarter())
# qu_tp <- m1dynq_(mr_str(), "qu", "tp", current_quarter())
# qu_aogd <- m1dynq_(mr_str(), "qu", "aogd", current_quarter())
# qu_qsd <- m1dynq_(mr_str(), "qu", "qsd", current_quarter())
# qu_sfd <- m1dynq_(mr_str(), "qu", "sfd", current_quarter())
# qu_tti <- m1dynq_(mr_str(), "qu", "tti", current_quarter())
# qu_pti <- m1dynq_(mr_str(), "qu", "pti", current_quarter())
# qu_du <- m1dynq_(mr_str(), "qu", "du", current_quarter())
# qu_cu <- m1dynq_(mr_str(), "qu", "cu", current_quarter())
# qu_pau <- m1dynq_(mr_str(), "qu", "pau", current_quarter())
# qu_veh <- m1dynq_(mr_str(), "qu", "veh", current_quarter())
# qu_ped <- m1dynq_(mr_str(), "qu", "ped", current_quarter())
# qu_cctv <- m1dynq_(mr_str(), "qu", "cctv", current_quarter())
# qu_reported <- m1dynq_(mr_str(), "qu", "reported", current_quarter())
# qu_resolved <- m1dynq_(mr_str(), "qu", "resolved", current_quarter())
# qu_outstanding <- m1dynq_(mr_str(), "qu", "outstanding", current_quarter())

```







One-Month Summary
=====================================

Row {data-height = 50}
-------------------------------------

### Performance <a id = "page_performance"></a>

Arterial performance measures (% change from previous month)

Row
-------------------------------------

### Throughput [vph] {.value-box}

```{r cache = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "tp", current_month()), 
                           "vph", as_int, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-bar-chart",
    color = BLUE
  )
})
```

### Arrivals on Green {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "aogd", current_month()), 
                           "aog", as_pct, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-car",
    color = BLUE
  )
})
```

### Progression Ratio {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "prd", current_month()), 
                           "pr", as_2dec, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-car",
    color = BLUE
  )
})
```

### Spillback Rate {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "qsd", current_month()), "qs_freq", as_pct, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-car",
    color = BLUE
  )
})
```

### Split Failures {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "sfd", current_month()), "sf_freq", as_pct, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-car",
    color = BLUE
  )
})
```

### Travel Time Index {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "mo", "tti", current_month()), 
                           "tti", as_2dec, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-dashboard",
    color = BLUE
  )
})
```

### Planning Time Index {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "mo", "pti", current_month()), 
                           "pti", as_2dec, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-dashboard",
    color = BLUE
  )
})
```


Row {data-height = 50}
-------------------------------------

### Volume-Based Measures

Corridor volumes (% change from previous month)

Row
-------------------------------------


### Traffic Volume [veh/day] {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = get_valuebox(m1dynq(mr_str(), "mo", "vpd", current_month()), 
                         "vpd", as_int, 
                         zone = zone_group(), mo = current_month(), break_ = FALSE),
    icon = "fa-area-chart",
    color = BLUE
  )
})
```

### AM Peak Volume [veh/hr] {.value-box}

```{r}
renderValueBox({
    valueBox(
        value = get_valuebox(m1dynq(mr_str(), "mo", "vphpa", current_month()), 
                             "vph", as_int, 
                             zone = zone_group(), mo = current_month(), break_ = FALSE),
        icon = "fa-area-chart",
        color = BLUE
    )
})
```

### PM Peak Volume [veh/hr] {.value-box}

```{r}
renderValueBox({
    valueBox(
        value = get_valuebox(m1dynq(mr_str(), "mo", "vphpp", current_month()), 
                             "vph", as_int, 
                             zone = zone_group(), mo = current_month(), break_ = FALSE),
        icon = "fa-area-chart",
        color = BLUE
    )
})
```

### {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = NULL,
    icon = NULL,
    color = "gray80"
  )
})
```

Row {data-height = 50}
-------------------------------------

### Equipment Measures

Device and Communications Uptime (% change from previous month)

Row
-------------------------------------

### Vehicle Detector Availability [manually collected] {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "mo", "veh", current_month()), 
                           "uptime", as_pct, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-car",
    color = BLUE
  )
})
```

### Pedestrian Detector Availability [manually collected] {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "mo", "ped", current_month()), 
                           "uptime", as_pct, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-child",
    color = BLUE
  )
})
```

### CCTV Availability {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = get_valuebox(m1dynq(mr_str(), "mo", "cctv", current_month()), "uptime", as_pct, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
    icon = "glyphicon-facetime-video",
    color = BLUE
  )
})
```

### Communications Uptime {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "cu", current_month()), "uptime", as_pct, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "ion-paper-airplane", #"fa-user" #
    color = BLUE
  )
})
```

### Pedestrian Detector Availability [beta] {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "mo", "pau", current_month()), 
                           "uptime", as_pct, 
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-child",
    color = BLUE
  )
})
```

Row {data-height = 50}
-------------------------------------

### Activity Measures

Activity measures from TEAMS and as reported by Traffic Engineers (% change from previous month)

Row
-------------------------------------

### TEAMS Tasks Reported This Month {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = get_valuebox(rename(m1dynq("cor", "mo", "events", current_month()), delta = delta.rep), 
                         "Reported", as_int, 
                           zone = zone_group(), mo = current_month(), break_ = FALSE),
    icon = "fa-area-chart",
    color = BLUE
  )
})
```

### TEAMS Tasks Resolved This Month {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = get_valuebox(rename(m1dynq("cor", "mo", "events", current_month()), delta = delta.res), "Resolved", as_int, 
                           zone = zone_group(), mo = current_month(), break_ = FALSE),
    icon = "fa-area-chart",
    color = BLUE
  )
})
```

### TEAMS Tasks Outstanding [Unresolved] {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = get_valuebox(rename(m1dynq("cor", "mo", "events", current_month()), delta = delta.out), "Outstanding", as_int, 
                           zone = zone_group(), mo = current_month(), break_ = FALSE),
    icon = "fa-area-chart",
    color = BLUE
  )
})
```

### {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = NULL,
    icon = NULL,
    color = "gray80"
  )
})
```









Quarter Summary
=====================================

Row {data-height = 50}
-------------------------------------

### Performance

Arterial performance measures (% change from previous quarter)

Row
-------------------------------------

### Throughput [vph] {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "tp", current_quarter()), "vph", as_int, 
                           zone = zone_group(), 
                           mo = current_month(), 
                           qu = current_quarter(), break_ = TRUE),
      icon = "fa-bar-chart",
      color = GDOT_BLUE
  )
})
```

### Arrivals on Green {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "aogd", current_quarter()), "aog", as_pct, 
                           zone = zone_group(), 
                           mo = current_month(), 
                           qu = current_quarter(), break_ = TRUE),
      icon = "fa-car",
      color = GDOT_BLUE_RGB
  )
})
```

### Progression Ratio {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "prd", current_quarter()), "pr", as_2dec, 
                           zone = zone_group(), 
                           mo = current_month(), 
                           qu = current_quarter(), break_ = TRUE),
      icon = "fa-car",
      color = GDOT_BLUE_RGB
  )
})
```

### Spillback Rate {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "qsd", current_quarter()), "qs_freq", as_pct, 
                           zone = zone_group(), 
                           mo = current_month(), 
                           qu = current_quarter(), break_ = TRUE),
      icon = "fa-car",
      color = GDOT_BLUE
  )
})
```

### Split Failures {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "sfd", current_quarter()), "sf_freq", as_pct, 
                           zone = zone_group(), 
                           mo = current_month(), 
                           qu = current_quarter(), break_ = TRUE),
      icon = "fa-car",
      color = GDOT_BLUE_RGB
  )
})
```

### Travel Time Index {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "tti", current_quarter()), "tti", as_2dec, 
                           zone = zone_group(), 
                           mo = current_month(), 
                           qu = current_quarter(), break_ = TRUE),
      icon = "fa-dashboard",
      color = GDOT_BLUE
  )
})
```

### Planning Time Index {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "pti", current_quarter()), "pti", as_2dec, 
                           zone = zone_group(), 
                           mo = current_month(), 
                           qu = current_quarter(), break_ = TRUE),
      icon = "fa-dashboard",
      color = GDOT_BLUE_RGB
  )
})
```


Row {data-height = 50}
-------------------------------------

### Volume-Based Measures

Corridor volumes (% change from previous quarter)

Row
-------------------------------------


### Traffic Volume [veh/day] {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = get_valuebox(m1dynq("cor", "qu", "vpd", current_quarter()), 
                         "vpd", as_int, 
                           zone = zone_group(), 
                           mo = current_month(), 
                           qu = current_quarter(), break_ = FALSE),
    icon = "fa-area-chart",
      color = GDOT_YELLOW_RGB
  )
})
```

### AM Peak Volume [veh/hr] {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "vphpa", current_quarter()), 
                           "vph", as_int, 
                           zone = zone_group(), 
                           mo = current_month(),
                           qu = current_quarter(), break_ = FALSE),
      icon = "fa-area-chart",
      color = GDOT_YELLOW
  )
})
```

### PM Peak Volume [veh/hr] {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "vphpp", current_quarter()), 
                           "vph", as_int, 
                         zone = zone_group(), 
                         mo = current_month(), 
                         qu = current_quarter(), break_ = FALSE),
      icon = "fa-area-chart",
      color = GDOT_YELLOW_RGB
  )
})
```


### {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = NULL,
    icon = NULL,
    color = "gray80"
  )
})
```

Row {data-height = 50}
-------------------------------------

### Equipment Measures

Device and Communications Uptime (% change from previous quarter)

Row
-------------------------------------

### Vehicle Detector Availability [manually collected] {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "veh", current_quarter()), 
                           "uptime", as_pct, 
                         zone = zone_group(), 
                         mo = current_month(), 
                         qu = current_quarter(), break_ = TRUE),
      icon = "fa-car",
      color = GDOT_BLUE
  )
})
```

### Pedestrian Detector Availability [manually collected] {.value-box}

```{r}

renderValueBox({
  valueBox(
    value = get_valuebox(m1dynq("cor", "qu", "ped", current_quarter()), 
                         "uptime", as_pct, 
                         zone = zone_group(), 
                         mo = current_month(), 
                         qu = current_quarter(), break_ = TRUE),
    icon = "fa-child",
      color = GDOT_BLUE_RGB 
  )
})
```

### CCTV Availability {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = get_valuebox(m1dynq("cor", "qu", "cctv", current_quarter()), 
                         "uptime", as_pct, 
                         zone = zone_group(), 
                         mo = current_month(), 
                         qu = current_quarter(), break_ = TRUE),
    icon = "glyphicon-facetime-video",
      color = GDOT_BLUE 
  )
})
```

### Communications Uptime {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "cu", current_quarter()), 
                           "uptime", as_pct, 
                         zone = zone_group(), 
                         mo = current_month(), 
                         qu = current_quarter(), break_ = TRUE),
      icon = "ion-paper-airplane",
      color = GDOT_BLUE_RGB
  )
})
```

### Pedestrian Detector Availability [beta] {.value-box}

```{r}

renderValueBox({
  valueBox(
    value = get_valuebox(m1dynq("cor", "qu", "pau", current_quarter()), 
                         "uptime", as_pct, 
                         zone = zone_group(), 
                         mo = current_month(), 
                         qu = current_quarter(), break_ = TRUE),
    icon = "fa-child",
      color = GDOT_BLUE
  )
})
```

Row {data-height = 50}
-------------------------------------

### Activity Measures

Activity measures from TEAMS and as reported by Traffic Engineers (% change from previous quarter)

Row
-------------------------------------

### TEAMS Tasks Reported This Quarter {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "qu", "reported", current_quarter()), 
                           "Reported", as_int, 
                         zone = zone_group(), 
                         mo = current_month(), 
                         qu = current_quarter(), break_ = FALSE),
      icon = "fa-area-chart",
      color = GDOT_YELLOW
  )
})
```

### TEAMS Tasks Resolved This Quarter {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = get_valuebox(m1dynq("cor", "qu", "resolved", current_quarter()), 
                         "Resolved", as_int, 
                         zone = zone_group(), 
                         mo = current_month(), 
                         qu = current_quarter(), break_ = FALSE),
    icon = "fa-area-chart",
      color = GDOT_YELLOW_RGB
  )
})
```

### TEAMS Tasks Outstanding [Unresolved] {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = get_valuebox(m1dynq("cor", "qu", "outstanding", current_quarter()), 
                         "Outstanding", as_int, 
                         zone = zone_group(), 
                         mo = current_month(), 
                         qu = current_quarter(), break_ = FALSE),
    icon = "fa-area-chart",
      color = GDOT_YELLOW
  )
})
```

### {.value-box}

```{r}
renderValueBox({
  valueBox(
    value = NULL,
    icon = NULL,
    color = "gray80"
  )
})
```









Summary Trend
=====================================

Row
-------------------------------------

### Performance

```{r summary_left_TEST, eval = TRUE, warning = FALSE}
fillCol(
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "tp", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "vph", "Throughput", RED2, fill_color_ = NULL, as_int) 
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "aogd", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "aog", "Arrivals on Green\nGoal: 80%", RED2, fill_color_ = LIGHT_RED, as_pct, ".1%", goal_ = 0.80) 
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "prd", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "pr", "Progression Ratio\nGoal: 1.2", RED2, fill_color_ = LIGHT_RED, as_2dec, ".2f", goal_ = 1.20) 
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "qsd", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "qs_freq", "Queue\nSpillback", RED2, fill_color_ = NULL, as_pct, ".1%")
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "sfd", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "sf_freq", "Split\nFailure", RED2, fill_color_ = NULL, as_pct, ".1%")
    }),
    renderPlotly({ 
        data.set <- m1dynq("cor", "mo", "tti", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "tti", "Travel Time\nIndex\nGoal: 2.00", RED2, fill_color_ = LIGHT_RED, as_2dec, ".2f", goal_ = 2.00) 
    }),
    renderPlotly({ 
        data.set <- m1dynq("cor", "mo", "pti", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "pti", "Planning Time\nIndex\nGoal: 2.00", RED2, fill_color_ = LIGHT_RED, as_2dec, ".2f", goal_ = 2.00)
    }),
    height = 700)
```


```{r summary_left, eval = FALSE, warning = FALSE}

# This is the good one

fillCol(
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "tp", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "vph", "Throughput", RED2, as_int) 
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "aogd", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "aog", "Arrivals on Green\nGoal: 80%", RED2, as_pct, ".1%", goal_ = 0.80) 
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "prd", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "pr", "Progression Ratio\nGoal: 80%", RED2, as_2dec, ".1f", goal_ = 1.20) 
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "qsd", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "qs_freq", "Queue\nSpillback", RED2, as_pct, ".1%")
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "sfd", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "sf_freq", "Split\nFailure", RED2, as_pct, ".1%")
    }),
    renderPlotly({ 
        data.set <- m1dynq("cor", "mo", "tti", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "tti", "Travel Time\nIndex\nGoal: 1.24", RED2, as_2dec, ".2f", goal_ = 1.24) 
    }),
    renderPlotly({ 
        data.set <- m1dynq("cor", "mo", "pti", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "pti", "Planning Time\nIndex\nGoal: 1.32", RED2, as_2dec, ".2f", goal_ = 1.32)
    }),
    height = 700)
```



### Volumes and Equipment

```{r summary_right_TEST, eval = TRUE, warning = FALSE}

fillCol(
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "vpd", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "vpd", "Daily\nVolume", GDOT_BLUE, fill_color_ = NULL, as_int)
    }),
    
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "vphpa", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "vph", "AM Hourly\nVolume", GDOT_BLUE, fill_color_ = NULL, as_int)
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "vphpp", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "vph", "PM Hourly\nVolume", GDOT_BLUE, fill_color_ = NULL, as_int)
    }),

    renderPlotly({ 
        data.set <- m1dynq("cor", "mo", "du", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "uptime.all", "Detector\nUptime\nGoal: 95%", ORANGE, fill_color_ = LIGHT_ORANGE, as_pct, ".1%", goal_ = 0.95)
    }),
    renderPlotly({ 
        data.set <- m1dynq("cor", "mo", "ped", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "uptime", "Ped Detector\nUptime\nGoal: 95%", ORANGE, fill_color_ = LIGHT_ORANGE, as_pct, ".1%", goal_ = 0.95)
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "cctv", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "uptime", "CCTV\nUptime\nGoal: 95%", ORANGE, fill_color_ = LIGHT_ORANGE, as_pct, ".1%", goal_ = 0.95)
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "cu", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "uptime", "Communications\nUptime\nGoal: 95%", ORANGE, fill_color_ = LIGHT_ORANGE, as_pct, ".1%", goal_ = 0.95)
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "pau", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot_beta(data.set, "uptime", "Ped Detector\nUptime [test]\nGoal: 95%", ORANGE, fill_color_ = LIGHT_ORANGE, as_pct, ".1%", goal_ = 0.95)
    }),
    height = 800)
```

```{r summary_right, eval = FALSE, warning = FALSE}

# This is the good one

fillCol(
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "vpd", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "vpd", "Daily\nVolume", GDOT_BLUE, as_int)
    }),
    
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "vphpa", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "vph", "AM Hourly\nVolume", GDOT_BLUE, as_int)
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "vphpp", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "vph", "PM Hourly\nVolume", GDOT_BLUE, as_int)
    }),

    renderPlotly({ 
        data.set <- m1dynq("cor", "mo", "du", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "uptime.all", "Detector\nUptime\nGoal: 95%", ORANGE, as_pct, ".1%", goal_ = 0.95)
    }),
    renderPlotly({ 
        data.set <- m1dynq("cor", "mo", "ped", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "uptime", "Ped Detector\nUptime\nGoal: 95%", ORANGE, as_pct, ".1%", goal_ = 0.95)
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "cctv", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "uptime", "CCTV\nUptime\nGoal: 95%", ORANGE, as_pct, ".1%", goal_ = 0.95)
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "cu", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "uptime", "Communications\nUptime\nGoal: 95%", ORANGE, as_pct, ".1%", goal_ = 0.95)
    }),
    renderPlotly({ 
        data.set <- m1dynq(mr_str(), "mo", "pau", first_month, current_month(), corridor = zone_group())
        shiny::validate(need(nrow(data.set) > 0, "No Data"))
        perf_plot(data.set, "uptime", "Ped Detector\nUptime [test]\nGoal: 95%", ORANGE, as_pct, ".1%", goal_ = 0.95)
    }),
    height = 800)
```









Performance
=====================================

Row
-------------------------------------

### Throughput (vph) {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "tp", current_month()), 
                           "vph", as_int,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-bar-chart"
  )
})
```

### Arrivals on Green {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "aogd", current_month()), 
                           "aog", as_pct,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-car"
  )
})
```

### Progression Ratio {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "prd", current_month()), 
                           "pr", as_2dec,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-car"
  )
})
```

### Spillback Rate {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "qsd", current_month()), 
                           "qs_freq", as_pct,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-car"
  )
})
```

### Split Failures {.value-box}

```{r}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "sfd", current_month()), 
                           "sf_freq", as_pct,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-car"
  )
})
```

### Travel Time Index {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "mo", "tti", current_month()), 
                           "tti", as_2dec,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-dashboard"
  )
})
```

### Planning Time Index {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "mo", "pti", current_month()), 
                           "pti", as_2dec,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-dashboard"
  )
})
```


Row {.tabset .tabset-fade}
-------------------------------------


### Throughput

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = TRUE, warning = FALSE}
renderPlotly({
    get_bar_line_dashboard_plot(m1dynq(mr_str(), "wk", "tp", first_month, endof_current_month()), 
                                m1dynq(mr_str(), "mo", "tp", current_month()), 
                                NULL,
                                "vph", "integer", highlight_color = RED2,
                                month_ = current_month(), zone_group_ = zone_group(),
                                x_bar_title = "Vehicles per Hour",
                                x_line1_title = paste(input$month, "Throughput (vph)"),
                                plot_title = "Throughput (peak veh/hr) Trend")
})
```


### Arrivals on Green

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, warning = FALSE}
renderPlotly({
    get_bar_line_dashboard_plot(m1dynq(mr_str(), "wk", "aog", first_month, endof_current_month()),
                                m1dynq(mr_str(), "mo", "aogd", current_month()), 
                                m1dynq(mr_str(), "mo", "aogh", current_month()),
                                "aog", "percent", highlight_color = RED2,
                                month_ = current_month(), zone_group_ = zone_group(),
                                x_bar_title = paste(input$month, "AOG"),
                                x_line1_title = "AOG Trend",
                                x_line2_title = paste(input$month, "AOG by TOD"),
                                plot_title = "Percent Arrivals on Green")
})
```

### Progression Ratio

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, warning = FALSE}
renderPlotly({
    get_bar_line_dashboard_plot(m1dynq(mr_str(), "wk", "pr", first_month, endof_current_month()),
                                m1dynq(mr_str(), "mo", "prd", current_month()), 
                                m1dynq(mr_str(), "mo", "prh", current_month()),
                                "pr", "decimal", highlight_color = RED2,
                                month_ = current_month(), zone_group_ = zone_group(),
                                x_bar_title = paste(input$month, "AOG"),
                                x_line1_title = "Progression Ratio Trend",
                                x_line2_title = paste(input$month, "Progression Ratio by TOD"),
                                plot_title = "Progression Ratio")
})
```


### Queue Spillback Rate

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = TRUE, warning = FALSE}
renderPlotly({
    get_bar_line_dashboard_plot(m1dynq(mr_str(), "wk", "qs", first_month, endof_current_month()), 
                                m1dynq(mr_str(), "mo", "qsd", current_month()), 
                                m1dynq(mr_str(), "mo", "qsh", current_month()),
                                "qs_freq", "percent", highlight_color = RED2,
                                month_ = current_month(), zone_group_ = zone_group(),
                                x_bar_title = paste(input$month, "Queue Spillback Rate"),
                                x_line1_title = "Queue Spillback Trend",
                                x_line2_title = paste(input$month, "Queue Spillback by TOD"),
                                plot_title = "Queue Spillback Rate")
})
```


### Split Failures

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, warning = FALSE}
renderPlotly({
    get_bar_line_dashboard_plot(m1dynq(mr_str(), "wk", "sf", first_month, endof_current_month()), 
                                m1dynq(mr_str(), "mo", "sfd", current_month()), 
                                m1dynq(mr_str(), "mo", "sfh", current_month()),
                                "sf_freq", "percent", highlight_color = RED2,
                                month_ = current_month(), zone_group_ = zone_group(),
                                x_bar_title = paste(input$month, "Split Failures Rate"),
                                x_line1_title = "Split Failures Trend",
                                x_line2_title = paste(input$month, "Split Failures by TOD"),
                                plot_title = "Split Failures Rate")
})
```


### Travel Time Metrics

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = TRUE, warning = FALSE}
renderPlotly({
    get_tt_plot(m1dynq("cor", "mo", "tti", first_month, current_month()), 
                m1dynq("cor", "mo", "ttih", current_month()), 
                m1dynq("cor", "mo", "pti", first_month, current_month()), 
                m1dynq("cor", "mo", "ptih", current_month()),
                month_ = current_month(),
                zone_group_ = zone_group(),
                x_bar_title = paste(input$month, "TTI & PTI"),
                x_line1_title = paste(input$month, "TTI by hr"),
                x_line2_title = paste(input$month, "PTI by hr"))
})
```









Volumes
=====================================

Row
-------------------------------------

### Traffic Volume [veh/day] {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
    value = get_valuebox(m1dynq(mr_str(), "mo", "vpd", current_month()), 
                         "vpd", as_int,
                           zone = zone_group(), mo = current_month(), break_ = FALSE),
    icon = "fa-area-chart"
  )
})
```

### AM Peak Volume [veh/hr] {.value-box}

```{r, warning = FALSE}
renderValueBox({
    valueBox(
        value = get_valuebox(m1dynq(mr_str(), "mo", "vphpa", current_month()), 
                             "vph", as_int,
                             zone = zone_group(), mo = current_month(), break_ = FALSE),
        icon = "fa-area-chart"
    )
})
```

### PM Peak Volume [veh/hr] {.value-box}

```{r, warning = FALSE}
renderValueBox({
    valueBox(
        value = get_valuebox(m1dynq(mr_str(), "mo", "vphpp", current_month()), 
                             "vph", as_int,
                             zone = zone_group(), mo = current_month(), break_ = FALSE),
        icon = "fa-area-chart"
    )
})
```

Row {.tabset .tabset-fade}
-------------------------------------

### Daily Volume

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = TRUE, warning = FALSE}
renderPlotly({ # Good version
    get_bar_line_dashboard_plot(m1dynq(mr_str(), "wk", "vpd", first_month, endof_current_month()), 
                                m1dynq(mr_str(), "mo", "vpd", current_month()), 
                                NULL,
                                "vpd", "integer", highlight_color = GDOT_BLUE,
                                month_ = current_month(), zone_group_ = zone_group(),
                                x_bar_title = "Vehicles per Day",
                                x_line1_title = paste(input$month, "Daily Volume (vpd)"),
                                plot_title = "Daily Volume (veh/day) Trend")
})
```


```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = FALSE, warning = FALSE}
### Monthly Change
renderPlotly({
    get_pct_ch_plot(m1dynq(mr_str(), "mo", "vpd", first_month, current_month()),
                    month_ = current_month(),
                    zone_group = zone_group())
})
```


```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = FALSE, warning = FALSE}
### Peak Hour Volumes
renderPlotly({
    am_vph_plot <- get_bar_line_dashboard_plot(
        m1dynq(mr_str(), "wk", "vphpa", first_month, endof_current_month()),
        m1dynq(mr_str(), "mo", "vphpa", current_month()),
        NULL,
        "vph", "integer", highlight_color = GDOT_BLUE,
        month_ = current_month(), zone_group_ = zone_group(),
        x_bar_title = "Vehicles per Hour",
        x_line1_title = paste(input$month, "Hourly Volume (vph)"),
        plot_title = "Peak Period Hourly Volume (veh/hr) Trend")
    
    pm_vph_plot <- get_bar_line_dashboard_plot(
        m1dynq(mr_str(), "wk", "vphpp", first_month, endof_current_month()),
        m1dynq(mr_str(), "mo", "vphpp", current_month()), 
        NULL,
        "vph", "integer", highlight_color = GDOT_BLUE,
        month_ = current_month(), zone_group_ = zone_group(),
        x_bar_title = "Vehicles per Hour",
        x_line1_title = paste(input$month, "Hourly Volume (vph)"),
        plot_title = "Peak Period Hourly Volume (veh/hr) Trend")
    subplot(am_vph_plot, pm_vph_plot, nrows = 1, margin = 0.04)
})
```



```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = FALSE, warning = FALSE}
### Hourly Volumes
renderPlotly({
    get_minmax_hourly_plot(m1dynq(mr_str(), "mo", "vph", first_month, current_month()),
                           month_ = current_month(), 
                           zone_group_ = zone_group())
})
```


### Pedestrians

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = TRUE, warning = FALSE}
renderPlotly({ # Good version
    get_bar_line_dashboard_plot(m1dynq(mr_str(), "wk", "papd", first_month, endof_current_month()), 
                                m1dynq(mr_str(), "mo", "papd", current_month()), 
                                NULL,
                                "papd", "integer", highlight_color = GDOT_BLUE,
                                month_ = current_month(), zone_group_ = zone_group(),
                                x_bar_title = "Ped Activations per Day",
                                x_line1_title = paste(input$month, "Ped Activations (papd)"),
                                plot_title = "Pedestrian Activations per Day (pa/day) Trend")
})
```








Equipment
=====================================

Row
-------------------------------------

### Vehicle Detector Availability {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "du", current_month()) %>% 
                          group_by(Corridor) %>% 
                          mutate(delta = uptime.all - lag(uptime.all)), 
                      "uptime.all", as_pct,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-car"
  )
})
```

### CCTV Availability {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
    value = get_valuebox(m1dynq("cor", "mo", "cctv", current_month()), 
                         "uptime", as_pct,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
    icon = "glyphicon-facetime-video" 
  )
})
```

### Pedestrian Detector Availability {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "mo", "ped", current_month()), 
                           "uptime", as_pct,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-child" 
  )
})
```

### Communications Uptime {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq(mr_str(), "mo", "cu", current_month()), 
                           "uptime", as_pct,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "ion-paper-airplane"
  )
})
```

### Pedestrian Detector Availability [beta] {.value-box}

```{r, warning = FALSE}
renderValueBox({
  valueBox(
      value = get_valuebox(m1dynq("cor", "mo", "pau", current_month()), 
                           "uptime", as_pct,
                           zone = zone_group(), mo = current_month(), break_ = TRUE),
      icon = "fa-child" 
  )
})
```


Row {.tabset .tabset-fade}
-------------------------------------

### Detector Uptime

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = TRUE, warning = FALSE}

renderPlotly({
    
    if (corridor() == "All Corridors") {

        get_cor_det_uptime_plot(m1dynq(mr_str(), "dy", "du", first_month, endof_current_month()), 
                                month_name = input$month,
                                month_ = current_month(), 
                                zone_group_ = zone_group())
        
    } else {
        
        get_uptime_plot(m1dynq("sig", "dy", "du", first_month, endof_current_month()), 
                                 m1dynq("sig", "mo", "du", current_month()), 
                                 var_ = "uptime.all", 
                                 num_format = "percent", 
                                 month_ = current_month(), 
                                 zone_group_ = zone_group(),
                                 x_bar_title = "Current Month Uptime",
                                 x_line1_title = "Daily Uptime",
                                 plot_title = "",
                                 goal = NULL)
    }
})
```

### Pedestrian Detector Uptime

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, warning = FALSE}
renderPlotly({
    
    weekly_ped_df <- rename(m1dynq("cor", "mo", "ped", first_month, current_month()), Date = Month)
    monthly_ped_df <- m1dynq("cor", "mo", "ped", current_month())
    get_bar_line_dashboard_plot(weekly_ped_df,
                                monthly_ped_df,
                                var_ = "uptime", num_format = "percent", highlight_color = BROWN,
                                month_ = current_month(), zone_group_ = zone_group(),
                                x_bar_title = paste(input$month, "Ped Det Uptime"),
                                x_line1_title = "Pedestrian Detector Uptime Trend",
                                plot_title = "Ped Detector Uptime")
})
```

### CCTV Uptime

```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, warning = FALSE}

renderPlotly({
    
    if (corridor() == "All Corridors") {
        
        weekly_cctv_df <- m1dynq("cor", "wk", "cctv", first_month, endof_current_month())
        monthly_cctv_df <- m1dynq("cor", "mo", "cctv", current_month())
        get_bar_line_dashboard_plot(weekly_cctv_df,
                                    monthly_cctv_df,
                                    var_ = "uptime",
                                    num_format = "percent",
                                    highlight_color = BROWN,
                                    month_ = current_month(), 
                                    zone_group_ = zone_group(),
                                    x_bar_title = paste(input$month, "CCTV Uptime"),
                                    x_line1_title = "CCTV Uptime Trend",
                                    plot_title = "CCTV Uptime")
    } else {
        
        plot_individual_cctvs(m1dynq("sig", "dy", "cctv", first_month, endof_current_month()), 
                              month_ = current_month(), 
                              #zone_group_ = zone_group(),
                              corridor_ = corridor())
    }
    
})
```




### Communications Uptime


```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = TRUE, warning = FALSE}

renderPlotly({

    if (corridor() == "All Corridors") {
        
        get_cor_comm_uptime_plot(m1dynq(mr_str(), "dy", "cu", first_month, endof_current_month()),
                                 m1dynq(mr_str(), "mo", "cu", current_month()),
                                 month_name = input$month,
                                 month_ = current_month(),
                                 zone_group_ = zone_group())
    } else {
        
        get_uptime_plot(m1dynq("sig", "dy", "cu", first_month, endof_current_month()), 
                        m1dynq("sig", "mo", "cu", current_month()), 
                        var_ = "uptime", 
                        num_format = "percent", 
                        month_ = current_month(), 
                        zone_group_ = zone_group(),
                        x_bar_title = "Current Month Uptime",
                        x_line1_title = "Daily Uptime",
                        plot_title = "",
                        goal = NULL)
    }
    
})
```




### Pedestrian Detector Uptime [beta]


```{r, fig.width = FIG_WIDTH, fig.height = FIG_HEIGHT, eval = TRUE, warning = FALSE}

renderPlotly({

    if (corridor() == "All Corridors") {
        
        get_cor_comm_uptime_plot(m1dynq(mr_str(), "dy", "pau", first_month, endof_current_month()),
                                 m1dynq(mr_str(), "mo", "pau", current_month()),
                                 month_name = input$month,
                                 month_ = current_month(),
                                 zone_group_ = zone_group())
    } else {
        
        get_uptime_plot(m1dynq("sig", "dy", "pau", first_month, endof_current_month()), 
                        m1dynq("sig", "mo", "pau", current_month()), 
                        var_ = "uptime", 
                        num_format = "percent", 
                        month_ = current_month(), 
                        zone_group_ = zone_group(),
                        x_bar_title = "Current Month Uptime",
                        x_line1_title = "Daily Uptime",
                        plot_title = "",
                        goal = NULL)
    }
    
})
```








Activity
=====================================

Row
-------------------------------------

### TEAMS Tasks Reported This Month {.value-box}

```{r, eval = TRUE, warning = TRUE}
renderValueBox({
  valueBox(
    value = get_valuebox(rename(m1dynq("cor", "mo", "events", current_month()), delta = delta.rep), 
                         "Reported", as_int,
                           zone = zone_group(), mo = current_month(), break_ = FALSE),
    icon = "fa-area-chart"
  )
})
```

### TEAMS Tasks Resolved This Month {.value-box}

```{r, eval = TRUE, warning = TRUE}
renderValueBox({
  valueBox(
    value = get_valuebox(rename(m1dynq("cor", "mo", "events", current_month()), delta = delta.res), 
                         "Resolved", as_int,
                           zone = zone_group(), mo = current_month(), break_ = FALSE),
    icon = "fa-area-chart"
  )
})
```

### TEAMS Tasks Outstanding (Unresolved) {.value-box}

```{r, eval = TRUE, warning = TRUE}
renderValueBox({
  valueBox(
    value = get_valuebox(rename(m1dynq("cor", "mo", "events", current_month()), delta = delta.out), 
                         "Outstanding", as_int,
                           zone = zone_group(), mo = current_month(), break_ = FALSE),
    icon = "fa-area-chart"
  )
})
```


Row {.tabset .tabset-fade data-height=800}
------------------------------------------

### TEAMS

```{r eval = TRUE, warning = FALSE}

fillRow(
    fillCol(
        renderPlotly({ 
            data.set <- gather_outstanding_events(
                filter(m1dynq("cor", "mo", "events", first_month, current_month()), 
                       Zone_Group == zone_group() & 
                           Month <= current_month())
            )
            shiny::validate(need(nrow(data.set) > 0, "No Data"))
            cum_events_plot(data.set)
        }),
        renderPlotly({ 
            data.set <- filter(teams_tables$type, Zone_Group == zone_group() & 
                                   Month == current_month())
            shiny::validate(need(nrow(data.set) > 0, "No Data"))
            plot_teams_tasks(data.set,
                             var_ = "Task_Type",
                             title_ = "Incidents This Month by Type (top), Source (bottom)", 
                             height_ = 250) 
        }), 
        renderPlotly({ 
            data.set <- filter(teams_tables$source, Zone_Group == zone_group() & 
                                   Month == current_month())
            shiny::validate(need(nrow(data.set) > 0, "No Data"))
            plot_teams_tasks(data.set,
                             var_ = "Task_Source", 
                             height_ = 150) 
        }), 
        height = 800, flex = c(NA, NA, 1)), 
        
    fillCol(
        renderPlotly({ 
            data.set <- filter(teams_tables$subtype, Zone_Group == zone_group() & 
                                   Month == current_month())
            shiny::validate(need(nrow(data.set) > 0, "No Data"))
            plot_teams_tasks(data.set,
                             var_ = "Task_Subtype",
                             title = "Incidents This Month by Subtype", 
                             textpos = "outside", 
                             height_ = 700) 
        }),
        height = 800))
```





### RTOP Activity Logs


#### **Download TSOS Monthly Reports (docx)**

`r lapply(rev(report_months[report_months >= '2018-01-01']), function(d) {
    Month_yyyy <- format(d, "%B %Y")
    yyyy_mm <- format(d, "%Y-%m")
    glue("[{Month_yyyy}](https://s3.amazonaws.com/gdot-devices/TSOS+Monthly+Meeting+Report+-+{yyyy_mm}.docx)")
}) %>% paste(collapse = " | ")`


#### **Download Traffic Engineers' Activity Reports (pdf)**

```{r}
zone_links2 <- function(corrs_, current_month_, rtop_) {
    lapply(corrs_, function(corr) {
        yyyy <- format(current_month_, "%Y")
        mm <- format(current_month_, "%m")
        
        corr_plus <- paste(yyyy, mm, gsub("/", "-", corr))
        corr_plus <- gsub(" ", "+", corr_plus)
        glue("[{corr}]({AWS_BUCKET}/{rtop_}/{yyyy}-{mm}/{corr_plus}.pdf)")
    }) %>% paste(collapse = " | ")
}
zl <- reactive(zone_links2(c("SR 13/42/155", "SR 141S", "SR 237", "SR 9-Atlanta"), current_month(), "RTOP1"))
```

**RTOP 1**

Zone 1: 
`r renderUI({get_progress_report_link("SR 13/42/155", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("SR 141S", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("SR 237", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("SR 9-Atlanta", current_month(), "RTOP1")})`

Zone 2: 
`r renderUI({get_progress_report_link("SR 120W", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("SR 280", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("SR 360", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("SR 3N", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("SR 92", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("SR 9N", current_month(), "RTOP1")})`

Zone 3: 
`r renderUI({get_progress_report_link("SR 138", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("SR 3S", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("SR 85", current_month(), "RTOP1")})`

Zone 8:
`r renderUI({get_progress_report_link("SR 92/140", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("Dunwoody-Brookhaven", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link("Sandy Springs", current_month(), "RTOP1")})`


**RTOP 2**

Zone 4: 
`r renderUI({get_progress_report_link("US 278", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 3", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 5", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 6", current_month(), "RTOP2")})`

Zone 5: 
`r renderUI({get_progress_report_link("SR 10", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 12", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 154", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 155S", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 42", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 8W-DeKalb", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 8W-Ponce", current_month(), "RTOP2")})`

Zone 6: 
`r renderUI({get_progress_report_link("SR 120E", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 140-Gwinnett", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 141N", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 20", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 8E", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link("SR 9-Alpharetta", current_month(), "RTOP2")})`

Zone 7: 
`r renderUI({get_progress_report_link("Zone 7", current_month(), "RTOP2")})`


**February 2019 and later**

`r renderUI({get_progress_report_link_by_zone("Zone 1", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link_by_zone("Zone 2", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link_by_zone("Zone 3", current_month(), "RTOP1")})` | 
`r renderUI({get_progress_report_link_by_zone("Zone 4", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link_by_zone("Zone 5", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link_by_zone("Zone 6", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link_by_zone("Zone 7", current_month(), "RTOP2")})` | 
`r renderUI({get_progress_report_link_by_zone("Zone 8", current_month(), "RTOP1")})`



Watchdog
=====================================


```{r watchdog}

#source("filter_SPM_alerts.R")
source("plot_functions.R")


# Main panel with tabs: Map, Table, Plots
fluidRow(
    column(width = 3,
           dateRangeInput("date_range", "Date Range:",
                        start = today() - days(28),
                        end = today(),
                        min = today() - days(365),
                        max = today(),
                        format = "mm/dd/yy", 
                        startview = "month", 
                        weekstart = 0,
                        separator = " - ")
    ),
    column(width = 3,
         selectInput("alert_type", "Alert:",
                     choices = c("No Camera Image",
                                 "Bad Vehicle Detection",
                                 "Bad Ped Detection",
                                 "Pedestrian Activations",
                                 "Force Offs",
                                 "Max Outs",
                                 "Count",
                                 "Missing Records"),
                     selected = "No Camera Image")
    ),
    column(width = 2,
        conditionalPanel("(input.alert_type == 'Missing Records') ||
                          (input.alert_type == 'Bad Vehicle Detection') ||
                          (input.alert_type == 'No Camera Image')",
             selectInput("phase.all", "Phase:",
                         choices = "All",
                         selected = "All")),
        conditionalPanel("(input.alert_type == 'Pedestrian Activations') || 
                          (input.alert_type == 'Force Offs') ||
                          (input.alert_type == 'Max Outs') ||
                          (input.alert_type == 'Count') ||
                          (input.alert_type == 'Bad Ped Detection')",
             selectInput("phase.eight", "Phase:",
                         choices = c("All", seq_len(8)),
                         selected = "All"))
    ),
    column(width = 3,
         textInput("id_filter", "Intersection Filter:")
    )
)
fluidRow(
    tabsetPanel(type = "tabs",
                tabPanel("Plot", 
                         helpText("Plotting up to 40 rows. See table tab for full list. Use the Intersection Filter above to narrow the list for plotting."),
                         renderPlot({
                             alerts_plot()
                         }, height = 600)
                ),
                tabPanel("Table", 
                         renderDataTable({
                             datatable(filtered_alerts()$table, escape = FALSE,
                                       extensions = 'Scroller', options = list(
                                           deferRender = TRUE,
                                           scrollY = 500,
                                           scroller = TRUE,
                                           searching = FALSE)
                            )
                        })
                )
    )
)



filtered_alerts <- reactive({

    alerts_by_date <- filter_alerts_by_date(alerts, input$date_range)
    
    ph <- if(input$phase.eight != "All") {
        input$phase.eight
    } else {
        "All"
    }

    filter_alerts(alerts_by_date, # filter_mr_data(alerts_by_date, zone_group())
                  input$alert_type,
                  input$zone_group,
                  ph, #input$phase,
                  input$id_filter)
})
    


alerts_plot <- reactive({

    dataset <- filtered_alerts()

    if (nrow(dataset$plot) > 0) {
        
        plot_df <- dataset$plot %>% 
            filter(SignalID %in% dataset$table$SignalID[1:40]) %>% 
            mutate(signal_phase = factor(signal_phase))
        
        if (nrow(plot_df) > 0) {
            plot_alerts(plot_df, input$date_range)
        } else {
            plot_empty(zone_group)
        }

    } else {
        plot_empty(zone_group)
    }
})
```







Signals List
=====================================

Row {data-height = 800}
-------------------------------------

```{r}
renderDataTable({
    
    datatable(filtered_corridors(),  #corr, 
              escape = FALSE,
              extensions = 'Scroller', 
              options = list(
                  deferRender = TRUE,
                  scrollY = 800,
                  scroller = TRUE,
                  searching = TRUE)
    )
})
```









Signal Details
=====================================

```{r signal_detail}
fillCol(height = 100,
renderUI({
    selectInput("signalid", "SignalID:",
                choices = c("Select", filtered_signalids()),
                selected = "",
                width = "600px")
}))


fillCol(height = 700,

    renderPlotly({
        sid <- sub(":.+", "", input$signalid)
        signal_dashboard_athena(sid, current_month())
    })
)
```










About
=====================================

### PERFORMANCE

#### **Throughput**

Throughput is a measure of efficiency. It is meant to represent the maximum
number of vehicles serviced through an intersection, and the corridor.

Throughput is calculated as the highest 15-minute volume in a day for
each intersection, converted to an hourly volume, averaged over the
corridor. Volumes come from high-resolution event logs from the controller,
which are stored in the ATSPM database.

#### **Arrivals on Green**

Arrivals on Green (AOG) is a measure of coordination. A percentage of 
vehicles arriving on green would be due to good offsets
and should be correlated with fewer stops and less delay.

AOG is calculated as the total number of vehicles arriving on green
divided by the total number of arrivals. It includes main street through phases,
during peak periods (6am-10am, 3pm-7pm) on Tuesdays, Wednesdays and
Thursdays.

The calculation takes the ATSPM data, aggregates the counts by cycle and
interval, and divides the counts in the green intervals by the total counts
for the cycle, averaged over all cycles.

#### **Progression Ratio**

Progression Ratio is arrivals on green (AOG) percentage divided by the percent green
for that phase. A ratio of greater than 1 means better than random arrivals.

... more here ...

#### **Queue Spillback Rate**

Queue Spillback Rate is an experimental measure of effectiveness.
It is a measure of unmet demand in a cycle. Based on the vehicle
dwell times over setback detectors on the main street through phases, each cycle is
evaluated for spillback. A spillback condition will be triggered by
occupancy readings above what is typical for setback detectors under
freely flowing conditions. 

Specifically, under freely flowing
conditions, the time between subsequent detector on and off events is typically
around 0.1 seconds for setback detectors. 
When the 95^th^ percentile detector occupancy duration increases above 3
seconds in a cycle, it will be assumed there is standing traffic on the setback
detector and a spillback event will be flagged for that phase in that
cycle.

The queue spillback rate is calculated as the number of spillback events
divided by the number of cycles for main street through phases.

#### **Split Failures**

Split failure is another measure of unmet demand. It identifies cycles where a
phase has unserved demand. A phase is flagged for split failure when a  detector on event occurs
before the start of red and the subsequent detector off event occurs after the next
start of green.

Because this measure requires stop
bar detection, it is only be calculated for side street and left
turn phases, i.e., all phases other than main street through phases.

Split Failure Percent is calculated as the number of split failures divided by the total
number of side street and left turn phases.

#### **Travel Time Index**

Travel Time Index (TTI) is a measure of delay on the corridor. It is the
ratio of travel time to free flow travel time.

Hourly travel time data from HERE queried from RITIS. Free flow travel
times are based on the "reference speed" value from HERE for each
segment. Travel time and free flow travel time are calculated for each
corridor by summing over all segments in the corridor for every hour in
the month.

An hourly Travel Time Index is then calculated for each corridor as the
average travel time for that hour of the day divided by the free flow
travel time for the corridor. The TTI for each hour is then averaged
over the day, weighted by the average hourly volume on the main street through phases
(from ATSPM) to get a TTI for the month (this gives more weight to peak
periods than off-peak periods).

#### **Planning Time Index**

The Planning Time Index (PTI) calculation uses the same data as the
Travel Time Index. However, instead of taking the average travel times
for each hour of the day, it takes the 90^th^ percentile for each hour,
over the days of the month. These 90^th^ percentile travel times are
then averaged over the day, weighted by the average hourly volume from
the main street through phases  (from ATSPM) to get a PTI for the month 
(this gives more weight to peak periods than off-peak periods).



### VOLUMES

#### **Daily Volume**

Volume is a measure of demand on a corridor. Total volume on main street 
through phases are summed over each Tuesday, Wednesday and Thursday,
and then averaged over all days in the month.

#### **Monthly Change**

Each bar in the Monthly Change plot shows the percent change in daily volume
compared with the previous month.

#### **Peak Hour Volumes**

The AM and PM average peak hour volumes are the average hourly volume
for the month over the AM and PM peak hours, respectively. 
Peak hours are 6am-10am, 3pm-7pm.

#### **Hourly Volumes**

The hourly volumes chart shows the range of hourly volumes for all
months from the beginning of the data set, superimposed with the hourly
volumes for the current month. It is meant to show the spread of each
hour and whether the current month is high or low compared to the
historical range.

### EQUIPMENT AND ACTIVITY

#### **Detector Uptime**

Detector Uptime is a measure of state-of-good-repair, which may be correlated to
other performance measures since failed detectors may negatively affect
performance.

Based on hourly volumes by detector, each hour is flagged if any of the
following conditions apply:

-   Missing data (volume = NA)

-   volume &gt; 1000

-   Absolute change in volume from previous hour &gt; 500

-   change in volume from previous hour = 0

For each day, if more than 40% of hours are flagged the detector is considered down for that day.
Separately, if the average absolute change from the previous hour, averaged over the day, is
greater than 200, the detector is considered down fot that day. The
uptime is the percentage of good days in the month. These thresholds are
all based on observation of the data and can be adjusted as necessary.

#### **Pedestrian Detector Uptime**

Pedestrian Detector Uptime is the percentage of pedestrian pushbuttons operational and is
reported by Corridor Managers from physically pressing each button in
the field at some point during the month and observing whether it is
working.

There are two reasons why this is currently done manually. The first is that
multiple push buttons are often physically wired into the same detector input,
making it impossible from the controller inputs to determine whether both push
buttons are working, or just one. The second is due to the relatively infrequent calls,
it is difficult to tell from the data whether there is no demand or whether the 
push button has failed.

#### **CCTV Uptime**

Through December 2017, CCTV Uptime was reported by Corridor Managers in their 
monthly reports. In January, 2018, it came from TSOS based on a manual check
of each camera during the month and reporting whether it returns an
image and whether it can be controlled (pan-tilt-zoom).

As of February 2018, CCTV Uptime is calculated based on the image returned from
the Georgia 511 website. If there is a live image, it will considered
working as of its "last modified" timestamp from the website.

#### **Communications Uptime**

This is calculated from gaps in the ATSPM high resolution data. Any gaps
in subsequent events greater than 15 minutes are considered to be due to 
communication loss. The sum of these gaps converted to a percent is the
daily communication uptime for that controller. If comms are lost for all
intersections, it is considered a system failure and that time is excluded
from the uptime calculation.

#### **Events Reported, Resolved, Outstanding**

Activity measures come from TEAMS reports from data as entered by corridor managers.
TEAMS is GDOT's ticketing and tracking system for signal equipment and 
incident-related activity.

All events reported in the month are counted, as are the number of
events resolved in the month. The number of outstanding events is the
cumulative sum of reported events less the cumulative sum of resolved
events.

#### **RTOP Activity Logs**

These are the monthly reports produced monthly by RTOP Corridor Managers detailing key activities, maintenance tasks and action items for the month.

